/** Fallback when hostname không khớp site nào */
export const defaultSiteProfile = {
  id: 'default',
  listingItemSelector: null,
  serverTabsSelector: null,
  networkIdleCapMs: 8000,
  scrollPasses: 1,
  skipHtmlFetchFallback: false,
  prepareListingPage: null,
  filterListingMatch: null,
};
