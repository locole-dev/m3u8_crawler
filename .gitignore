# Dependencies
node_modules/

# Agent toolkit (local dev only, not for deploy)
.agent/

# Secrets & local env
.env
.env.local
.env.*.local

# Generated playlist / scrape output
playlist.m3u
playlist.json
film.m3u
film.json
*-test.m3u
*-test.json
tmp-*.mjs
film.crawl.lock
film-crawl-log.txt

# Logs & debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS
.DS_Store
Thumbs.db

# IDE (JetBrains / VS Code — bỏ dòng nếu team commit cấu hình)
.idea/
.vscode/

# Playwright (nếu sau này dùng trace / report mặc định)
test-results/
playwright-report/
blob-report/
